<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.globaltelecomunicationinc.conjugationstation.MainActivity">

    <ImageView
        android:id="@+id/ivLoadingGame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:alpha="0"
        android:src="@drawable/loading_game" />

    <ImageButton
        android:id="@+id/btnQuit"
        android:layout_width="125dip"
        android:layout_height="30dip"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@+id/btnStart"
        android:scaleType="fitXY"
        android:layout_marginEnd="16dp"
        android:padding="0dp"
        android:src="@drawable/btn_quitter"/>

    <ImageButton
        android:id="@+id/btnStart"
        android:layout_width="125dip"
        android:layout_height="30dip"
        android:layout_marginStart="18dp"
        android:scaleType="fitXY"
        android:padding="0dp"
        android:src="@drawable/btn_commencer"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="20dp" />

</RelativeLayout>
